# ADWP — Pending Tasks Tracker

**Created:** 2026-03-11
**Last Updated:** 2026-03-11 — **ALL TASKS COMPLETE** (24/24) — Verified

---

## P0 — Critical (Blocks Pilot Readiness)

- [x] **P0-1:** Implement Kafka producers/consumers across all services (24 events from P2) — **DONE**: Created `@adwp/kafka` shared package (KafkaModule, KafkaProducerService, 24 topic constants, typed event payloads). Wired into all 8 services: hybrid app bootstrap, producer emit calls replacing all placeholders, consumer controllers in analytics-service and notification-service.
- [x] **P0-2:** Fix JWT — add `jti` claim, confirm RS256 signing, implement Redis-based token revocation — **DONE**: Added `jti` (randomUUID) to JWT payload, confirmed RS256 with public/private key pair, created `TokenRevocationService` with Redis-backed JTI blocklist (auto-expiring TTL), integrated revocation check in JwtStrategy.validate() and logout flow.
- [x] **P0-3:** Add audit event publishing (`audit.event.recorded`) from every state-changing operation — **DONE**: AuditService in @adwp/kafka, 17 audit calls across all 7 services (tenant, auth, subscription, agent-registry, integration-hub, workflow-service, notification-service). Covers: CRUD ops, login/logout, config changes, escalations, human tasks.
- [x] **P0-4:** Implement real notification sending (SendGrid email adapter in notification-service) — **DONE**: Created EmailChannel with @sendgrid/mail, graceful dev mode when API key missing, integrated into NotificationsService with proper pending→sent/failed lifecycle.

## P1 — High (Required for Completeness)

- [x] **P1-1:** Add `POST /v1/auth/mfa/verify` endpoint (TOTP verification) — **DONE**: Added otplib, created VerifyMfaDto, setupMfa/verifyMfa/enableMfa methods in AuthService, POST /v1/auth/mfa/setup and /verify endpoints. Auto-enables MFA on first successful TOTP verification.
- [x] **P1-2:** Implement LLM provider failover in `ai-runtime/llm/gateway.py` (OpenAI → Anthropic cascade) — **DONE**: Added PROVIDER_PRIORITY list, `_complete_with_failover()` and `_complete_with_tools_failover()` helpers with automatic cascade through available providers.
- [x] **P1-3:** Add Recharts visualizations to analytics pages (bar, line, pie charts) — **DONE**: Created 4 reusable chart components (BarChart, LineChart, PieChart, AreaChart) with custom tooltips and Tailwind colors. Updated performance, cost, and audit pages with charts + KPI cards + mock data fallback.
- [x] **P1-4:** Implement WebSocket for dashboard live feed + task queue real-time updates — **DONE**: Created Socket.IO gateway (EventsGateway) on /ws namespace, WsKafkaConsumer bridging 6 Kafka topics to WS events, tenant-scoped rooms. Frontend hooks (useWebSocket, useDashboardFeed, useTaskQueue) with react-query cache invalidation and live connection status indicators.
- [x] **P1-5:** Build dedicated Agent Config page (`/workforce/{agentId}/config`) with tabbed layout (Identity, Policy, Escalation, Integrations, History) — **DONE**: Created 5 tab components (IdentityTab, PolicyTab, EscalationTab, IntegrationsTab, HistoryTab), agent-config.api.ts, AgentFullConfig types, main config page with tab navigation.
- [x] **P1-6:** Add deep health checks in all services (DB, Redis, Kafka connectivity probes) — **DONE**: Added @nestjs/terminus to all 8 services. GET /v1/health (liveness) + GET /v1/health/ready (readiness) with PrismaHealthIndicator, KafkaHealthIndicator (all services), and RedisHealthIndicator (auth-service). K8s-ready 503 on failure.
- [x] **P1-7:** Add `AgentConfigHistory` model to Prisma schema + config versioning logic — **DONE**: Added AgentConfigHistory model with version tracking, DlqEntry model, timescaledb extension, changeReason field in DTO, snapshot creation in updateAgentConfig.
- [x] **P1-8:** Fix HTTP 204 status codes on DELETE/logout endpoints (auth logout, auth api-keys delete, tenant user delete, integration connection delete) — **DONE**: Added @HttpCode(HttpStatus.NO_CONTENT) to all 4 endpoints across auth-service, tenant-service, and integration-hub.

## P2 — Medium (Production Readiness)

- [x] **P2-1:** Implement integration connectors for AI agents (start with Gmail + HubSpot) — **DONE**: Created GmailConnector (googleapis, sendEmail/listEmails/getEmail), HubSpotConnector (REST, contacts/deals CRUD), ConnectorFactory, ConnectorsService with token resolution + audit logging, 8 API endpoints under /connections/:id/actions/.
- [x] **P2-2:** Add database persistence to AI runtime (replace in-memory dicts with PostgreSQL/Redis) — **DONE**: Created db/ package (SQLAlchemy async models: AgentExecution, ConversationHistory, AgentMetrics), repository.py (CRUD + Redis working memory), integrated into OrchestrationEngine and MemoryManager with graceful degradation to in-memory fallback.
- [x] **P2-3:** Build Notification Center UI (bell icon + badge count + dropdown in header) — **DONE**: Created NotificationBell (with 30s polling unread count), NotificationDropdown (380px, scrollable, click-outside-close), NotificationItem (color-coded by type), notifications API functions, integrated into header.
- [x] **P2-4:** Add Global Search (Cmd+K command palette) — **DONE**: Created CommandPaletteProvider (context + Cmd+K shortcut), CommandPalette (modal with debounced search, keyboard nav, recent searches in localStorage), SearchResult (highlighted text, category badges). Integrated into dashboard layout and header.
- [x] **P2-5:** Implement OAuth token auto-refresh scheduled job (every 5 min) in integration-hub — **DONE**: Added @nestjs/schedule, TokenRefreshService with @Cron('*/5 * * * *'), queries expiring tokens, refreshes via provider OAuth2 endpoints, updates DB + token vault, emits Kafka event.
- [x] **P2-6:** Implement named retry profiles + per-provider circuit breaker configs in `packages/utils` — **DONE**: Created retry-profiles.ts (aggressive/standard/conservative/none + withRetry helper), circuit-breaker.ts (CLOSED→OPEN→HALF_OPEN state machine, per-provider configs for 10 providers).
- [x] **P2-7:** Add missing sidebar sub-routes (`/tasks/queue`, `/tasks/history`, `/workforce/active`) — **DONE**: Added collapsible sub-menu sections to sidebar (Tasks, Workforce, Analytics). Created 3 new pages: Task Queue (filterable table), Task History (completed/failed executions), Active Agents (card grid with status).
- [x] **P2-8:** Implement CSAT survey dispatch in customer support agent — **DONE**: Created CSATSurveyTool with structured survey payload (1-5 scale), notification-service HTTP integration, integrated into support_agent resolution flow with auto-dispatch on resolved tickets.
- [x] **P2-9:** Add `POST /v1/integrations/webhooks` endpoint — **DONE**: Created IntegrationWebhook Prisma model, WebhooksService (create/list/delete), WebhooksController with POST/GET/DELETE endpoints, CreateWebhookDto with validation.
- [x] **P2-10:** Rename `/analytics/audit` to `/analytics/compliance` + add `?format=pdf` export support — **DONE**: Created /analytics/compliance page, old /audit redirects for backwards compat, updated all tab navs + command palette. Added Export dropdown with CSV download + PDF via window.print().
- [x] **P2-11:** Convert Human Task Resolution from full page to 480px drawer overlay — **DONE**: Created TaskDrawer component (480px slide-over, backdrop blur, smooth translateX transition, close on ESC/backdrop click). Integrated into live tasks page — row click opens drawer instead of navigating. Full page route preserved for direct access.
- [x] **P2-12:** Add KPI change-vs-yesterday indicators on dashboard cards — **DONE**: Added ChangeIndicator component with green/red/gray arrows + percentage, invertColor for metrics where "up" is bad (cost, escalations, pending). Mock yesterday data for all 5 KPIs.
- [x] **P2-13:** Convert department filter on catalog page from dropdown to pill tabs — **DONE**: Replaced <select> dropdown with horizontal pill buttons (rounded-full, overflow-x-auto for mobile). Selected: bg-primary-600 text-white. Short labels for pills, full labels for cards.
- [x] **P2-14:** Add subscribe confirmation modal with integration requirements — **DONE**: Modal shows agent info, lists required integrations with Connected/Not Connected badges, amber warning when missing, Subscribe button disabled if requirements not met.
- [x] **P2-15:** Add performance charts (30-day line chart) to Agent Profile page — **DONE**: Added 30-day LineChart with tasks completed (solid) + avg response time (dashed), 3 summary stat cards, mock data with weekday/weekend variation.
- [x] **P2-16:** Add `DlqEntry` model to Prisma schema for Dead Letter Queue persistence — **DONE** (completed as part of P1-7)
- [x] **P2-17:** Add `timescaledb` extension to Prisma schema — **DONE** (completed as part of P1-7)
- [x] **P2-18:** Enhance PII redactor (TypeScript) with IBAN, passport, GCC ID, IP patterns — **DONE**: Added IBAN, credit card (Visa/MC/Amex/Discover), UAE/Saudi/Bahrain/Kuwait/Qatar GCC ID, passport, IPv4, IPv6 patterns. Standalone redactPii() function with per-pattern toggles via RedactOptions.
- [x] **P2-19:** Add Reassign button to Human Task Resolution UI — **DONE**: Added Reassign button (amber, UserPlus icon) with popover modal (user/team selector, reason field), reassignHumanTask API, Toast notification system (ToastProvider + useToast hook).
- [x] **P2-20:** Implement export CSV/PDF handlers in audit log page (currently empty placeholders) — **DONE**: CSV export (Blob + createObjectURL download, escaped fields), PDF export (window.print with hidden print-ready table, @media print CSS). Both with loading states and toast feedback.
